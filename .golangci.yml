version: "2"

linters:
  default: standard
  enable:
    - misspell
    - revive
  exclusions:
    presets:
      - comments
      - std-error-handling
    paths:
      - data

formatters:
  enable:
    - gofmt
    - goimports
